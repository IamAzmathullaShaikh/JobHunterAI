fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
prometheus-fastapi-instrumentator>=6.0.0
# JobHunterAI core – we assume it's available as a submodule or installed via editable install
# For local dev, you can do: pip install -e ../jobhunterai_core
